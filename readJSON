First install Pipeline Utility Steps.

here is the code to read the json files

def apps = readJSON file: 'test.json'		 
		apps.apps.each { 
			det -> println "${det}"
		}
    
    
We can also read csv file




		             def csvs = readCSV file: 'test.csv'		 
		            csvs.each { 
			        det -> println "${det}"
		            }
