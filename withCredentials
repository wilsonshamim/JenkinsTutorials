Go to pipeline syntex
select "withCredentials: bind to variable"
click on ADD and select appropriate type. 
I selected, username and passwor seperated
entered usename,password variable and entered ADD key
then generate the script.

withCredentials([usernamePassword(credentialsId: '81dd9bce-1bed-415a-87ac-945b1b3a9fdc', passwordVariable: 'PASSWORD',
usernameVariable: 'USERNAME')]) {
    // some block
    
}
